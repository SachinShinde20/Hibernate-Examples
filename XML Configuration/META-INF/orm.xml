<?xml version="1.0" encoding="UTF-8"?>

<!-- Hibernate ORM mapping file using JPA XML format -->
<!-- Maps a Java class (Student) to a database table and its columns -->

<entity-mappings
    version="2.2"
    xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence/orm
                        http://xmlns.jcp.org/xml/ns/persistence/orm_2_2.xsd">

    <!-- Maps the Java class to a database table -->
    <entity class="com.HibernetExamples.Student" access="PROPERTY">
        <!-- access="PROPERTY" tells Hibernate to use getter/setter methods instead of direct field access -->

        <!-- Optional: Custom table name in the database -->
        <table name="Student_Records"/>

        <attributes>
            <!-- Primary key mapping -->
            <id name="id">
                <!-- Maps the property "id" to the column "Student_id" -->
                <column name="Student_id"/>
            </id>

            <!-- Other fields/properties mapping -->

            <basic name="name">
                <column name="Student_name"/>
            </basic>

            <basic name="course">
                <column name="Student_course"/>
            </basic>

            <basic name="city">
                <column name="Student_city"/>
            </basic>

            <basic name="mobileNo">
                <column name="Student_mobile_Number"/>
            </basic>
        </attributes>
    </entity>
</entity-mappings>



<!-- 
🌟 Summary Notes on XML Mapping:

✔ <entity> → Maps Java class to a DB table
✔ class → Fully qualified class name (with package)
✔ access="PROPERTY" → Hibernate uses getter/setter methods instead of fields

✔ <table name="..."/> → Optional, only needed if table name ≠ class name

✔ <attributes> → Contains all property mappings (fields)

✔ <id> → Maps the primary key property
✔ <basic> → Maps a simple (non-primary) property
✔ <column name="..."/> → Maps Java property to DB column
     ➤ Optional if property name = column name
     ➤ Add constraints like:
         • nullable="false" → NOT NULL
         • unique="true" → UNIQUE
         • length="100" → VARCHAR(100)
         • column-definition="varchar(50) default 'Unknown'" → default value

✅ Important:
✔ If your table name = class name and column names = property names,
   then <table>, <basic>, and <column> tags are OPTIONAL!
   Hibernate will auto-map them by convention.

Example:
   public class Student {
       private int id;         // maps to column: id
       private String name;    // maps to column: name
   }

✔ You still need <id> and <basic>, unless you use annotations.

 -->
